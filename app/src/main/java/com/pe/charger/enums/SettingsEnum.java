package com.pe.charger.enums;

public enum SettingsEnum {
    IP("ip"),
    CURR_LIM("currentLim"),
    CURR_CHOSEN("currentChosen"); //выбранный ток заряда на слайдере

    SettingsEnum(String set) {
    }
}
